<Window x:Class="PL.Call.CallWindow"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Call"
        mc:Ignorable="d"
        Title="CallWindow" Height="450" Width="800"
        Loaded="Window_Loaded"
        Closed="Window_Closed"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
        FlowDirection="RightToLeft">
    <Window.Resources>
        <Style x:Key="myTextBlockStyle" TargetType="TextBlock">
            <Setter Property="Visibility" Value="{Binding ButtonText, Converter={StaticResource ConvertUpdateToVisibleKey}}" />
        </Style>
        <Style x:Key="myLabelStyle" TargetType="Label">
            <Setter Property="Visibility" Value="{Binding ButtonText, Converter={StaticResource ConvertUpdateToVisibleKey}}" />
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="190"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Content="מספר קריאה:" Grid.Row="0" Grid.Column="0" Visibility="{Binding ButtonText, Converter={StaticResource ConvertUpdateToTrueKey}}"/>
        <TextBlock Grid.Row="0" Grid.Column="1"
              Text="{Binding CurrentCall.Id, Mode=TwoWay, NotifyOnValidationError=true, ValidatesOnExceptions=true}"
              Visibility="{Binding ButtonText, Converter={StaticResource ConvertUpdateToTrueKey}}"/>
        <Label Content="סוג קריאה:" Grid.Row="1" Grid.Column="0"/>
        <ComboBox Grid.Row="1" Grid.Column="1"
          SelectedValue="{Binding CurrentCall.CallType, Mode=TwoWay, NotifyOnValidationError=true, ValidatesOnExceptions=true}"
          ItemsSource="{Binding Source={StaticResource CallTypeCollectionKey}}" Height="26" Grid.RowSpan="2" VerticalAlignment="Top"/>

        <Label Content="תאור קריאה:" Grid.Row="2" Grid.Column="0"/>
        <TextBox Grid.Row="2" Grid.Column="1"
              Text="{Binding CurrentCall.CallDescription, Mode=TwoWay, NotifyOnValidationError=true, ValidatesOnExceptions=true}"/>

        <Label Content="כתובת קריאה:" Grid.Row="3" Grid.Column="0"/>
        <TextBox Grid.Row="3" Grid.Column="1"
              Text="{Binding CurrentCall.CallAddress, Mode=TwoWay, NotifyOnValidationError=true, ValidatesOnExceptions=true}"/>

        <Label Content="קו רוחב:" Grid.Row="4" Grid.Column="0" Style="{StaticResource myLabelStyle}"/>
        <TextBlock Grid.Row="4" Grid.Column="1"
              Style="{StaticResource myTextBlockStyle}"
              Text="{Binding CurrentCall.Latitude, Mode=TwoWay, NotifyOnValidationError=true, ValidatesOnExceptions=true}"/>

        <Label Content="קו אורך:" Grid.Row="5" Grid.Column="0" Style="{StaticResource myLabelStyle}"/>
        <TextBlock Grid.Row="5" Grid.Column="1"
              Style="{StaticResource myTextBlockStyle}"
              Text="{Binding CurrentCall.Longitude, Mode=TwoWay, NotifyOnValidationError=true, ValidatesOnExceptions=true}"/>

        <Label Content="זמן פתיחת קריאה:" Grid.Row="6" Grid.Column="0" Style="{StaticResource myLabelStyle}"/>
        <TextBlock Grid.Row="10" Grid.Column="1"
        Style="{StaticResource myTextBlockStyle}"
         Text="{Binding CurrentCall.OpeningTime, Mode=TwoWay, NotifyOnValidationError=true, ValidatesOnExceptions=true}"
           />
        <Label Content="זמן מקסימלי לסיום:" Grid.Row="7" Grid.Column="0"/>
        <xctk:DateTimePicker
        Value="{Binding CurrentCall.MaxTimeFinishCall, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
        Format="Custom"
        FormatString="dd/MM/yyyy HH:mm"
        Grid.Row="6" Grid.Column="1"/>

        <Label Content="מרחק מקסימלי לקריאה במטרים:" Grid.Row="8" Grid.Column="0"/>
        <TextBox Grid.Row="8" Grid.Column="1"
              Text="{Binding CurrentVolunteer.MaxDistanceForCall, Mode=TwoWay, NotifyOnValidationError=true, ValidatesOnExceptions=true}"/>

        <Label Content="סטטוס קריאה:" Grid.Row="9" Grid.Column="0"/>
        <ComboBox Grid.Row="9" Grid.Column="1"
               SelectedValue="{Binding CurrentCall.StatusCall, Mode=TwoWay, NotifyOnValidationError=true, ValidatesOnExceptions=true}"
               ItemsSource="{Binding Source={StaticResource StatusCallCollectionKey}}"/>
        
        <Button Grid.Row="13" Grid.Column="1" Height="50" Margin="50,20,200,20"
             Content="{Binding ButtonText}" Click="btnAddUpdate_Click"/>
    </Grid>
</Window>
